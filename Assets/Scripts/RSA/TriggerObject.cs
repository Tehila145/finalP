using UnityEngine;

public class TriggerObject : MonoBehaviour
{
    //public BookInteractionClosed bookInteractionClosed;

    void OnMouseDown() 
    {
        // Assuming this object's click triggers the pillars to appear
        //bookInteractionClosed.GenerateAndDisplayPillars();
    }
}